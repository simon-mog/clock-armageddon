<UserControl x:Class="AnalogClock.View.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnalogClock.View"
             mc:Ignorable="d">
    <DockPanel>
        <Grid DockPanel.Dock="Right">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <RepeatButton Grid.Row="0" x:Name="UpButton" Content="▲" FontSize="9"/>
            <RepeatButton Grid.Row="1" x:Name="DownButton" Content="▼" FontSize="9"/>
        </Grid>
        <TextBox x:Name="ValueTextBox" VerticalContentAlignment="Center"/>
    </DockPanel>
</UserControl>
